<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.dao.mapper.LinkLocaleStatsMapper">

    <select id="listLocaleStatsByShortLink" resultType="com.xmon.shanlink.dao.entity.LinkLocaleStatsDO">
        SELECT full_short_url AS fullShortUrl, province, SUM(cnt) AS cnt
        FROM t_link_locale_stats
        WHERE full_short_url = #{fullShortUrl}
          AND del_flag = 0
          AND date BETWEEN #{startDate} AND #{endDate}
        GROUP BY province
    </select>

    <insert id="shortLinkLocaleState">
        INSERT INTO t_link_locale_stats (full_short_url, date, cnt, province, city, adcode, country, create_time, update_time, del_flag)
        VALUES (#{linkLocaleStats.fullShortUrl}, #{linkLocaleStats.date}, #{linkLocaleStats.cnt},
                #{linkLocaleStats.province}, #{linkLocaleStats.city}, #{linkLocaleStats.adcode},
                #{linkLocaleStats.country}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkLocaleStats.cnt}, update_time = NOW()
    </insert>

</mapper>
