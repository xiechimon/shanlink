<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.dao.mapper.LinkNetworkStatsMapper">

    <select id="listNetworkStatsByShortLink" resultType="com.xmon.shanlink.dao.entity.LinkNetworkStatsDO">
        SELECT full_short_url AS fullShortUrl, network, SUM(cnt) AS cnt
        FROM t_link_network_stats
        WHERE full_short_url = #{fullShortUrl}
          AND del_flag = 0
          AND date BETWEEN #{startDate} AND #{endDate}
        GROUP BY network
    </select>

    <insert id="shortLinkNetworkState">
        INSERT INTO t_link_network_stats (full_short_url, date, cnt, network, create_time, update_time, del_flag)
        VALUES (#{linkNetworkStats.fullShortUrl}, #{linkNetworkStats.date}, #{linkNetworkStats.cnt},
                #{linkNetworkStats.network}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkNetworkStats.cnt}, update_time = NOW()
    </insert>

</mapper>
