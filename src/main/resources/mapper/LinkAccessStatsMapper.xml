<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.dao.mapper.LinkAccessStatsMapper">

    <select id="listStatsByShortLink" resultType="com.xmon.shanlink.dao.entity.LinkAccessStatsDO">
        SELECT full_short_url AS fullShortUrl, date, pv, uv, uip, hour, weekday
        FROM t_link_access_stats
        WHERE full_short_url = #{fullShortUrl}
          AND del_flag = 0
          AND date BETWEEN #{startDate} AND #{endDate}
        ORDER BY date ASC
    </select>

    <insert id="shortLinkStats">
        INSERT INTO t_link_access_stats (full_short_url, date, pv, uv, uip, hour, weekday, create_time, update_time, del_flag)
        VALUES (#{linkAccessStats.fullShortUrl}, #{linkAccessStats.date}, #{linkAccessStats.pv},
                #{linkAccessStats.uv}, #{linkAccessStats.uip}, #{linkAccessStats.hour},
                #{linkAccessStats.weekday}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE
            pv       = pv + #{linkAccessStats.pv},
            uv       = uv + #{linkAccessStats.uv},
            uip      = uip + #{linkAccessStats.uip},
            update_time = NOW()
    </insert>

</mapper>
