<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.project.dao.mapper.LinkMapper">

    <select id="listGroupShortLinkCount"
            resultType="com.xmon.shanlink.project.dto.resp.LinkGroupCountQueryRespDTO">
        SELECT gid, COUNT(*) AS short_link_count
        FROM t_link
        WHERE gid IN
        <foreach collection="gidList" item="gid" open="(" separator="," close=")">
            #{gid}
        </foreach>
        AND del_flag = 0
        GROUP BY gid
    </select>

</mapper>
