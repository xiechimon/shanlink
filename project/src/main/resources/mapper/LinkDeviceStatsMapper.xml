<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.project.dao.mapper.LinkDeviceStatsMapper">

    <select id="listDeviceStatsByShortLink" resultType="com.xmon.shanlink.project.dao.entity.LinkDeviceStatsDO">
        SELECT full_short_url AS fullShortUrl, device, SUM(cnt) AS cnt
        FROM t_link_device_stats
        WHERE full_short_url = #{fullShortUrl}
          AND del_flag = 0
          AND date BETWEEN #{startDate} AND #{endDate}
        GROUP BY device
    </select>

    <insert id="shortLinkDeviceState">
        INSERT INTO t_link_device_stats (full_short_url, date, cnt, device, create_time, update_time, del_flag)
        VALUES (#{linkDeviceStats.fullShortUrl}, #{linkDeviceStats.date}, #{linkDeviceStats.cnt},
                #{linkDeviceStats.device}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkDeviceStats.cnt}, update_time = NOW()
    </insert>

</mapper>
