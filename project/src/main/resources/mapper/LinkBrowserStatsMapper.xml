<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.project.dao.mapper.LinkBrowserStatsMapper">

    <select id="listBrowserStatsByShortLink" resultType="com.xmon.shanlink.project.dao.entity.LinkBrowserStatsDO">
        SELECT full_short_url AS fullShortUrl, browser, SUM(cnt) AS cnt
        FROM t_link_browser_stats
        WHERE full_short_url = #{fullShortUrl}
          AND del_flag = 0
          AND date BETWEEN #{startDate} AND #{endDate}
        GROUP BY browser
    </select>

    <insert id="shortLinkBrowserState">
        INSERT INTO t_link_browser_stats (full_short_url, date, cnt, browser, create_time, update_time, del_flag)
        VALUES (#{linkBrowserStats.fullShortUrl}, #{linkBrowserStats.date}, #{linkBrowserStats.cnt},
                #{linkBrowserStats.browser}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkBrowserStats.cnt}, update_time = NOW()
    </insert>

</mapper>
