<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmon.shanlink.project.dao.mapper.LinkOsStatsMapper">

    <select id="listOsStatsByShortLink" resultType="com.xmon.shanlink.project.dao.entity.LinkOsStatsDO">
        SELECT full_short_url AS fullShortUrl, os, SUM(cnt) AS cnt
        FROM t_link_os_stats
        WHERE full_short_url = #{fullShortUrl}
          AND del_flag = 0
          AND date BETWEEN #{startDate} AND #{endDate}
        GROUP BY os
    </select>

    <insert id="shortLinkOsState">
        INSERT INTO t_link_os_stats (full_short_url, date, cnt, os, create_time, update_time, del_flag)
        VALUES (#{linkOsStats.fullShortUrl}, #{linkOsStats.date}, #{linkOsStats.cnt},
                #{linkOsStats.os}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkOsStats.cnt}, update_time = NOW()
    </insert>

</mapper>
